# Solo se usa si Railway NO detecta Dockerfile
[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "chmod +x bin/railway-start.sh",
]

[start]
cmd = "sh bin/railway-start.sh"
