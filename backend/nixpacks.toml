[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "composer install --no-dev --optimize-autoloader && php bin/console cache:warmup --env=prod && php bin/console doctrine:migrations:migrate --no-interaction --env=prod && php -S 0.0.0.0:$PORT -t public/"
